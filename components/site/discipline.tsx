const RULES = [
  {
    title: '30 минут на первый контакт',
    body: 'Таймер стартует при захвате, ночью — в 10:00. Подтверждение только пересылкой отправленного клиенту сообщения: нажатие кнопки ничего не значит. Не успел — лид возвращается в очередь и достаётся другому.',
  },
  {
    title: 'Красный список контактов',
    body: 'Три уровня: никогда, только старший, временно до даты. Сопоставление по user_id, а не по нику, — смена username не помогает. Флаг закрывает контакт сразу во всех лидах, включая активные.',
  },
  {
    title: 'Очки вместо штрафов',
    body: 'За захват лида — ноль. Ответ клиента +15, принятая старшим передача +25, сделка +50, упущенный по таймеру лид −5. Рейтинг обнуляется первого числа: новичок каждый месяц стартует на равных.',
  },
  {
    title: 'Дешёвые внешние API',
    body: 'Пул ключей Trustat с автоматической ротацией и резервом 20 % квоты под приоритетные запросы. Экономный режим ИИ размечает очевидную рекламу формулами, слабые лиды в модель не уходят вовсе.',
  },
]

export function Discipline() {
  return (
    <div className="grid gap-4 sm:grid-cols-2">
      {RULES.map((rule) => (
        <article key={rule.title} className="flex flex-col gap-2 rounded-lg border border-border bg-card p-5">
          <h3 className="text-base font-medium text-foreground text-balance">{rule.title}</h3>
          <p className="text-sm leading-relaxed text-muted-foreground text-pretty">{rule.body}</p>
        </article>
      ))}
    </div>
  )
}
